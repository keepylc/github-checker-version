services:
  bot:
    build: .
    container_name: 3xui-notifier
    restart: unless-stopped
    env_file: .env
    volumes:
      # Персистентное хранилище repos.json и versions.json
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - DATA_DIR=/app/data
